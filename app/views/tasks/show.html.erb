<h2><%= @task.title %></h2>
<%= link_to 'Edit this task', edit_task_path %>
<%= link_to 'Delete task', task_path(@task),
    data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>
<p><%= @task.details %></p>
<p><%= @task.completed == true ? 'Done' : 'Not done' %></p>
<%= link_to 'Back to list', tasks_path %>
