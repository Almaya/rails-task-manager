<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.label :completed unless @task.new_record? %>
  <%= f.text_field :completed unless @task.new_record? %>
  <%= f.submit %>
<% end %>
